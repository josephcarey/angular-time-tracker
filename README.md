# angular-time-tracker

## Setup

### Make a database
- create a local database named `time_tracker`.
- Run the code from [database.sql](database.sql) in Postico to create the tables and add some test data.
